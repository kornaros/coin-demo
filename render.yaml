services:
  - type: web
    name: coin-demo
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: voila app.ipynb --port 10000 --no-browser --Voila.allow_origin='*'
    plan: free
