services:
  - type: web
    name: coin-rules-app
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: voila main.ipynb --port=$PORT --no-browser --Voila.configuration.allow_origin="*"
